<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.ict.ex.mapper.BoardMapper">

	<select id="getList" resultType="BoardVO">
		<![CDATA[
			select * from mvc_board
		 ]]>
	</select>

	<!-- BoardVO read(int bno); -->
	<select id="read" resultType="BoardVO">
		 <![CDATA[
			select * from mvc_board where bid = #{bno}
		 ]]>
	</select>

	<!-- int delete(int bno); -->
	<delete id="delete">
        <![CDATA[
        	delete from mvc_board where bid = #{bno}
        ]]>
	</delete>
	
	<insert id="write" >
		 <![CDATA[
			insert into mvc_board (bid, bname, btitle, bcontent, bgroup, bstep, bindent) values (mvc_board_seq.nextval,#{btitle},#{bname},#{bcontent},0,0, 0)
		 ]]>
	</insert>
	
	<update id="modify" >
		 <![CDATA[
			update mvc_board set bName = #{bname}, bTitle = #{btitle},bContent = #{bcontent} where bId= #{bid}
		 ]]>
	</update>
</mapper>